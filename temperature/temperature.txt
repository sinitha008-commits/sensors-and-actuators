int tempPin = A0;

void setup() {
  Serial.begin(9600);
}

void loop() {
  float voltage = analogRead(tempPin) * 5.0 / 1023.0;
  float tempC = (voltage - 0.5) * 100;
  Serial.println(tempC);
  delay(1000);
}